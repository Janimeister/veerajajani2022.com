import { ImagePathPipePipe } from './image-path.pipe';

describe('ImagePathPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ImagePathPipePipe();
    expect(pipe).toBeTruthy();
  });
});
